version: 1
applications:
  - appRoot: promptopia
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npx next build
    artifacts:
      baseDirectory: .next
      files:
        - "**/*"
    cache:
      paths:
        - node_modules/**/*
